<h1>Add a new movie list</h1>
<div class="container">
<div>
<%= simple_form_for(@list) do |f| %>
  <%= f.input :name %>
  <%= f.submit %>
  <% end %>
  </div>
</div>

<%= link_to "Back to lists",  lists_path %>
