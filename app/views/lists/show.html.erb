
<h1><%= @list.name %>🎬</h1>

<div class="container">
<% @movies.each do |movie| %>
<div class=" container card mb-4">

  <p><%= image_tag movie.poster_url, width:200, height:200 %></p>
  <p><%= movie.title  %></p>
  <p>rating: <%= movie.rating %></p>
  <p> <%= Bookmark.find_by(movie_id: movie.id).comment %></p>
 </div>
<% end %>
</div>


<%= simple_form_for [@list, @bookmark ] do |f| %>
  <%= f.input :comment, placeholder: "Add a nice comment" %>
  <%= f.association :movie %>
  <%= f.button :submit, class: "btn btn-light" %>
<% end %>
